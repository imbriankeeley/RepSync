package com.repsync.app.ui.theme

import androidx.compose.ui.graphics.Color

// Background
val BackgroundPrimary = Color(0xFF000000)
val BackgroundSurface = Color(0xFF1A1A1A)
val BackgroundCard = Color(0xFF2C2C2E)
val BackgroundCardElevated = Color(0xFF3A3A3C)

// Primary (muted sage/olive green)
val PrimaryGreen = Color(0xFF8DAF8E)
val PrimaryGreenDark = Color(0xFF7E9D7C)

// Destructive (muted red/pink)
val DestructiveRed = Color(0xFFC48B8B)

// Text
val TextOnDark = Color(0xFFFFFFFF)
val TextOnDarkSecondary = Color(0xFFB0B0B0)
val TextOnLight = Color(0xFF1C1C1E)
val TextOnLightSecondary = Color(0xFF6C6C6E)

// Cards (light variant for exercise table areas)
val CardLight = Color(0xFFD1D1D6)
val CardLightSecondary = Color(0xFFC7C7CC)

// Input fields
val InputBackground = Color(0xFF48484A)

// Calendar highlight
val CalendarWorkoutDay = Color(0xFF8DAF8E)

// Checkmark
val CheckmarkGreen = Color(0xFF6BBF6B)

// Divider
val Divider = Color(0xFF48484A)
